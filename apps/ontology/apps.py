from django.apps import AppConfig


class OntologyConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.ontology'
    verbose_name = 'Semantic Digital Twin Service (SVC-ONTO)'
